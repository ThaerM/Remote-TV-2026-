name: remote_tv_2026
description: "Remote TV 2026 - a universal smart TV remote and companion app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  flutter_secure_storage: ^11.2.0
  logging: ^1.3.0
  multicast_dns: ^0.3.3
  basic_utils: ^5.8.2
  protobuf: ^6.1.0
  shared_preferences: ^2.3.3
  crypto: ^3.0.6
  package_info_plus: ^10.2.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3

flutter:
  uses-material-design: true
